import ColorLab from './ColorLab.jsx'

// ColorLab renders its own LabShell (title, subtitle, back-to-Labs link,
// undo/redo, mode toggle) - this page is just the /color-lab and
// /labs/color route target.
export default function ColorLabPage() {
  return <ColorLab />
}
